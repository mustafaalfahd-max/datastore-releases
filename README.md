# DataStore — التوزيع والتحديثات

هذا المستودع يستضيف ملف الإصدار (`version.json`) وحزم التثبيت لبرنامج **الأرشفة (DataStore)**.

- **للتثبيت الأول:** حمّل أحدث `datastore-setup-x.y.z.exe` من صفحة [الإصدارات (Releases)](../../releases/latest).
- **التحديث:** يتم تلقائياً داخل البرنامج (يقرأ `version.json` من هذا المستودع).

يبقى الكود المصدري خاصاً؛ هنا فقط ملفات التوزيع.

الدعم الفني / واتساب: **07844114487**
